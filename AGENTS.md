# 《废土中继》项目执行规则

## 权威文档

开始任务前，读取与任务相关的已确认策划、技术、计划、素材和验收文档。项目治理详细记录见 `docs/superpowers/specs/2026-07-17-agent-collaboration-governance-design.md`。

## 单主智能体模式

- 本项目默认由主智能体独立承担制作人、策划、架构、开发、测试和最终验收职责。
- 默认禁止启动子智能体。
- 只有重大任务可拆成两个以上完全独立、可真正并行且不修改同一文件组的工作时，主智能体才可临时启动最多两个子智能体。
- 子智能体只执行明确合同；不得改变方案、架构、验收标准或仓库状态。

## 任务分级

### 快速任务

适用于参数、文案、小型 Bug、单脚本局部修改和简单 UI。流程为：直接修改 → 最小测试 → 完成。不得启动子智能体、全项目分析、无关重构或完整回归。

### 标准任务

适用于中型功能、两到三个模块相关、普通场景交互和局部系统调整。流程为：明确验收标准 → 制订方案 → 实现 → 相关测试 → 自检。默认不启动子智能体。

### 重大任务

适用于核心玩法、战斗/状态机/存档、大型架构调整或多个独立模块。主智能体先确定完整架构和接口；仅满足“真正独立并行”条件时才可临时使用最多两个子智能体。

## 标准执行流程

1. 简要确认目标、实现/非实现范围、验收标准和受影响模块；已明确时不重复长篇分析。
2. 检查修改文件、公共接口、场景/资源/存档/输入影响、测试需求和更小的安全方案。
3. 在已确认架构内直接实现，保持变更小而可审查，不做无关重构或额外功能。
4. 按风险执行最小充分测试：语法/解析、启动、相关场景冒烟、核心流程；只有必要时做完整回归。
5. 主智能体按验收标准自检；只有明确失败项才返工，不因主观优化无限延长。
6. 最终仅汇报完成内容、修改文件、测试结果、已知限制和确实需要的后续事项。

## 迭代节奏与验证粒度（2026-07-18 补充）

- 迭代开发优先形成玩家可操作、可观察的最小玩法闭环；不得为了先完成内部接口、抽象或文档而延后核心可玩路径。
- 同一功能块内允许连续完成多个局部改动；不得默认在每个私有接口、方法或小型数据字段变动后中断开发来执行构建、测试或汇报。
- **全量自检只在一个迭代的开发内容全部完成后开始**，包括完整构建、全部相关 headless 套件、Godot 启动、可见玩法路径和回归检查。
- 开发中只允许三类即时检查：代码无法继续时的语法/编译诊断、正在修复的明确失败，以及伤害/状态机/资源写入等高风险逻辑的单个定向测试；这些检查不得扩展为全量流程。
- 长迭代应按“可玩闭环 → 补强机制 → 完整自检 → 用户验收”组织；若需要改变已确认玩法范围或验收标准，仍须先取得用户确认并同步权威文档。

## 不可突破的项目规则

- 任何玩法、范围、架构、依赖、素材授权、验收标准或迭代顺序的实质偏离，必须暂停并请求用户确认；确认后先更新权威文档。
- 开发不得用硬编码、临时替代或范围削减绕开已确认技术架构。
- 自动化测试不能替代 Godot 运行验证和策划可见验收。
- 用户明确验收通过前，不得提交或推送；只有主智能体可执行最终仓库操作。
- 长时间执行时，主智能体应在关键完成、发现问题、偏离和验证完成时主动汇报。
